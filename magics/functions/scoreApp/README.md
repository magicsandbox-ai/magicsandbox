Score a Magic App based on user satisfaction.

**Argument:** an object with keys:

- `score` (number) (required): Score to apply to the Magic App.
- `app` (string) (required): The Magic App to score.

This function does not return anything.
